version: '3.8'

services:
  web-training:
    build: .
    container_name: ctf-student-api
    ports:
      - "6767:6767"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    command: python app.py
